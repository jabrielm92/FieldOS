# ============================================================
# FieldOS Backend — Environment Variables
# Set these in Railway → Service → Variables tab.
# For local dev: copy to backend/.env and fill in values.
# Never commit a real .env to version control.
#
# Railway auto-provides:
#   MONGO_URL  — add the MongoDB plugin and Railway injects this
#   REDIS_URL  — add the Redis plugin and Railway injects this
#   PORT       — Railway injects this; uvicorn reads it automatically
# ============================================================

# ── Runtime ─────────────────────────────────────────────────
ENVIRONMENT=development

# ── MongoDB ─────────────────────────────────────────────────
# Railway: provided automatically by the MongoDB plugin — do not set manually
# Local dev: mongodb://localhost:27017
MONGO_URL=mongodb://localhost:27017
DB_NAME=fieldos

# ── Security ─────────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=CHANGE_ME_USE_A_STRONG_RANDOM_SECRET_AT_LEAST_32_CHARS_IN_PROD

# Optional bootstrap superadmin (dev/bootstrap only)
# Leave blank to disable automatic bootstrap user creation.
DEFAULT_SUPERADMIN_EMAIL=
DEFAULT_SUPERADMIN_PASSWORD=
DEFAULT_SUPERADMIN_NAME=Platform Admin

# ── Twilio (SMS + Voice) ─────────────────────────────────────
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_MESSAGING_SERVICE_SID=MGxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── ElevenLabs (Voice AI TTS) ────────────────────────────────
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# ── OpenAI (AI SMS + Voice logic) ───────────────────────────
OPENAI_API_KEY=sk-...

# ── Stripe (Payments) ────────────────────────────────────────
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ── Resend (Transactional Email) ─────────────────────────────
RESEND_API_KEY=re_...

# ── App URLs ─────────────────────────────────────────────────
# APP_BASE_URL: public URL of THIS backend service.
#   Railway: set to the backend's Railway-generated URL, e.g. https://fieldos-backend.up.railway.app
#   Used by Twilio for webhooks and for customer-facing links (portal, invoice, tracking).
APP_BASE_URL=https://your-backend.up.railway.app

# FRONTEND_URL: public URL of the frontend service.
#   Railway: set to the frontend's Railway-generated URL or your custom domain.
FRONTEND_URL=https://your-frontend.up.railway.app

# Comma-separated allowed CORS origins (must include the frontend URL).
CORS_ORIGINS=https://your-frontend.up.railway.app

# ── Redis ────────────────────────────────────────────────────
# Railway: provided automatically by the Redis plugin — do not set manually
# Local dev: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0
